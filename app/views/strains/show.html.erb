<% provide(:title, @strain.title) %>
<% provide(:canonical_url, canonical_url) %>

<%=
  download_buttons([
    [strain_url(@strain, format: :json), 'file-code', 'JSON']
  ])
%>
<h1><%= @strain.title %></h1>
<hr/>
<%= render(partial: 'name_box') %>
<%= render(partial: 'warnings') %>
<%= render(partial: 'strain_info', locals: { stand_alone: true }) %>
<%= render(partial: 'genomics') %>
<%= render(partial: 'metadata') %>
<%= render(partial: 'shared/cc') %>

