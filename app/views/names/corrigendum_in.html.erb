<h1><%= @name.name_html %></h1>
<hr/>

<%= simple_form_for(@name, url: corrigendum_name_path(@name), html: { method: :post }) do |f| %>
  <h4>
    Corrigendum in <%= link_to(@publication.short_citation, @publication) %>
  </h4>
  <%= f.hidden_field(:corrigendum_in_id) %>
  <%=
    f.input(
      :corrigendum_from, required: true, autofocus: true,
      label: 'Original name'
    )
  %>
  <%= f.button :submit, 'Submit' %>
<% end %>
