<% @sort ||= nil %>

<%= link_to author, class: 'btn btn-secondary', style: 'margin: 0.2em;' do %>
  <%= author.full_name %>
  <span class='badge badge-pill badge-light'><%=
    @sort == 'names' ? author.name_count : author.publications.count
  %></span>
<% end %>
